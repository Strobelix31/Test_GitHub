package Arrays;

import java.util.Arrays;
import java.util.List;

public class losses {
	

	public static void trimNonGrowingNumbers(List<Double> numbers) {
		  int lastIndex = 0; 							// Index der letzten Zahl, die noch in der Liste bleiben soll
		  for (int i = 1; i < numbers.size(); i++) { 	// Schleife über alle Zahlen in der Liste
			  	if (numbers.get(i) < numbers.get(lastIndex)) {
			  		
			  	}
		  }
	} 
		   
	public static void main(String[] args) {
	   
	
	}		 
}